# SW_Bootcamp_13기_A반_5팀 요구사항 명세서

## 프로젝트 개요

2D LG Guys는 Controller Pi의 카메라 기반 pose estimation 입력을 `PlayerControls`로 변환해 2D 캐릭터를 조작하는 파티형 장애물 게임입니다. 프론트엔드는 React 19, Vite, TypeScript, Phaser로 화면과 게임 렌더링을 담당하고, 백엔드는 FastAPI와 WebSocket으로 게임 상태, 컨트롤 입력, 맵 규칙, 게임 루프를 처리합니다.

## 요구사항 목록

| 요구사항 ID | 요구사항 내용 | 검증 기준 (완료 조건) | 구현/테스트 근거 | 시연 타임스탬프 |
| --- | --- | --- | --- | --- |
| R-01 | 사용자는 시작 화면에서 게임 소개와 조작 가이드를 확인하고 설정 화면으로 이동할 수 있다. | 브라우저 접속 시 StartGuide가 표시되고 시작 액션 후 Setup 화면으로 전환된다. | `frontend/src/App.tsx`, `frontend/src/screens/StartGuide.tsx`, `frontend/src/screens/StartGuide.test.tsx` | N/A |
| R-02 | 사용자는 게임 시작 전 3개 맵 중 하나를 선택할 수 있다. | Setup 화면에 꼬리잡기, 땅따먹기, 별모으기 맵이 표시되고 선택한 `mapId`가 백엔드 `GameState`에 반영된다. | `backend/game/map.py`, `frontend/src/screens/SetupScreen.tsx`, `frontend/src/screens/SetupScreen.test.tsx` | N/A |
| R-03 | 사용자는 게임 시작 전 플레이어 수를 1명부터 4명까지 선택할 수 있다. | `playerCount` 선택 시 백엔드가 선택 인원 수만큼 `players`와 `controllers`를 생성하고 프론트엔드가 이를 표시한다. | `backend/game/game_loop.py`, `backend/test_game_loop.py`, `frontend/src/screens/SetupScreen.tsx` | N/A |
| R-04 | 사용자는 활성 플레이어의 닉네임을 설정할 수 있다. | Setup 화면에서 입력한 닉네임이 `/game/nicknames`를 통해 저장되고 이후 상태와 결과 화면에 유지된다. | `backend/game/game_loop.py`, `frontend/src/screens/SetupScreen.test.tsx`, `frontend/src/screens/ResultScreen.test.tsx` | N/A |
| R-05 | 프론트엔드는 백엔드 API와 WebSocket을 사용하는 게임 클라이언트로 동작한다. | `BackendGameClient`가 `/health`, `/maps`, `/game`, `/game/setup`, `/game/nicknames`, `/game/start`, `/game/reset`, `/ws/game`을 호출한다. | `frontend/src/App.tsx`, `frontend/src/api/backendGameClient.ts`, `frontend/src/api/backendGameClient.test.ts` | N/A |
| R-06 | 백엔드는 Health, 맵 조회, 게임 상태 조회, 설정, 닉네임 변경, 시작, 리셋 API를 제공한다. | `/health`, `/maps`, `/game`, `/game/setup`, `/game/nicknames`, `/game/start`, `/game/reset` 라우트가 선언되어 있고 JSON 상태 반환 함수로 구현된다. | `backend/main.py`, `backend/test_main_routes.py` | N/A |
| R-07 | 프론트엔드는 WebSocket으로 백엔드 게임 상태를 구독할 수 있다. | `/ws/game` 연결 직후 현재 `GameState`를 수신하고 이후 게임 루프 상태 변경이 브로드캐스트된다. | `backend/main.py`, `frontend/src/api/backendGameClient.ts`, `frontend/src/api/backendGameClient.test.ts` | N/A |
| R-08 | Controller Pi는 WebSocket으로 MoveNet pose keypoint와 hand landmark를 전송할 수 있다. | `/ws/controller/pi_1`~`/ws/controller/pi_4` 연결을 허용하고 유효한 keypoints 및 선택적 hands 입력을 해당 `player_1`~`player_4` 컨트롤로 반영한다. | `backend/main.py`, `backend/game/state.py`, `backend/test_connections.py` | N/A |
| R-09 | MoveNet keypoint는 좌우 이동, 점프, 앉기 컨트롤로, hand landmark는 잡기 컨트롤로 변환된다. | 어깨 기울기, 손목/팔꿈치 높이, 어깨 높이, hand landmark 기반 두 손 모으기 판정 결과가 `PlayerControls(moveX, jump, duck, grab)`와 `motion`으로 반환된다. hand landmark가 부족한 경우 기존 keypoint 기반 fallback은 유지한다. | `backend/game/pose_action.py`, `backend/test_pose_action.py` | 00:09~00:44 |
| R-10 | 게임 시작은 선택된 Controller Pi가 모두 연결된 경우에만 가능하다. | 선택된 `pi_1..pi_n` 중 미연결 항목이 있으면 `/game/start`가 `CONTROLLERS_NOT_READY`로 거절되고, 모두 연결되면 countdown 상태가 된다. | `backend/main.py`, `backend/game/game_loop.py`, `backend/test_game_loop.py`, `frontend/src/screens/SetupScreen.test.tsx` | N/A |
| R-11 | 게임 화면은 백엔드 `GameState`에 따라 캐릭터 위치, 점수, 연결 상태, 이벤트를 표시한다. | `/ws/game`으로 수신한 player 좌표, grabbing 상태, score, controller status가 React HUD와 Phaser 렌더링에 반영된다. | `frontend/src/screens/GamePlayScreen.tsx`, `frontend/src/screens/GamePlayScreen.test.tsx`, `frontend/src/game/FallGuysScene.ts`, `frontend/src/game/playerRenderMetrics.test.ts` | N/A |
| R-12 | 꼬리잡기 맵은 잡기 동작과 거리 조건으로 꼬리 소유권을 이전한다. | 꼬리 미보유 플레이어가 꼬리 보유자 근처에서 grab하면 `tail.grabbed` 이벤트가 발생하고 `hasTail`이 이전된다. | `backend/game/physics.py`, `backend/test_physics.py`, `frontend/src/game/mapRenderers/tailTagRenderer.ts` | 00:47~01:10 |
| R-13 | 땅따먹기 맵은 발판 접촉으로 타일 소유권과 점수를 갱신한다. | 플레이어가 점령 가능한 타일을 밟으면 `territory.captured` 이벤트가 발생하고 타일 소유자와 점수가 갱신된다. | `backend/game/physics.py`, `backend/test_physics.py`, `frontend/src/game/mapRenderers/territoryCaptureRenderer.ts` | 01:11~01:32 |
| R-14 | 별모으기 맵은 별 수집과 재배치를 처리한다. | 플레이어가 별 위치에 도달하면 `star.collected` 이벤트가 발생하고 점수 및 다음 별 위치가 갱신된다. | `backend/game/physics.py`, `backend/test_physics.py`, `frontend/src/game/mapRenderers/starCollectRenderer.ts` | 01:33~01:52 |
| R-15 | 게임 종료 시 라운드 순위와 점수를 표시한다. | 종료 이벤트의 rankings를 기반으로 Result 화면에 순위, 점수, 다음 맵 선택 버튼이 표시된다. | `backend/test_game_loop.py`, `frontend/src/screens/ResultScreen.test.tsx`, `frontend/src/match/matchProgress.ts` | N/A |
| R-16 | 최대 3라운드까지 누적 점수를 계산하고 최종 순위를 표시한다. | 라운드별 점수가 같은 플레이어 누적 점수에 더해지고 3라운드 후 최종 결과 화면이 표시된다. | `frontend/src/match/matchProgress.test.ts`, `frontend/src/screens/ResultScreen.test.tsx`, `frontend/src/App.tsx` | N/A |
| R-17 | 게임 리셋 후 설정 또는 시작 화면으로 돌아갈 수 있다. | 게임 화면/결과 화면의 리셋 액션 실행 시 백엔드 상태가 초기화되고 Setup 또는 Start 화면이 표시된다. | `frontend/src/App.tsx`, `frontend/src/api/backendGameClient.ts`, `frontend/src/api/backendGameClient.test.ts`, `backend/main.py` | N/A |
| R-18 | 프론트엔드 자동 테스트는 API 클라이언트, 게임 규칙, 렌더링, 화면 흐름을 검증한다. | `cd frontend && npm run test` 실행 시 Vitest 테스트가 통과한다. | `frontend/src/**/*.test.ts`, `frontend/src/**/*.test.tsx` | N/A |
| R-19 | 프론트엔드 production build가 가능해야 한다. | `.nvmrc`와 `frontend/package.json` engines가 Node.js 18.20.4 기준을 명시하고, `cd frontend && npm run build` 명령이 오류 없이 완료된다. | `.nvmrc`, `frontend/package.json`, `frontend/package-lock.json` | N/A |
| R-20 | 백엔드 소스와 단위 테스트는 Python 검증을 통과해야 한다. | `python -m compileall backend`와 `cd backend && python -m unittest discover` 명령이 오류 없이 완료된다. | `backend/*.py`, `backend/game/*.py`, `backend/test_*.py` | N/A |
| R-21 | 제출물만으로 설치, 빌드, 실행 방법을 재현할 수 있어야 한다. | `RUN.md`에 의존성, 버전 고정, 설치, 실행, 환경 변수, 외부 자원, 테스트 방법이 포함되고, runbook 계약 테스트가 해당 문서/파일 구성을 검증한다. | `RUN.md`, `.nvmrc`, `frontend/.env.local.example`, `backend/requirements.txt`, `benchmark/requirements.txt`, `backend/test_runbook_contract.py` | N/A |
| R-22 | 백엔드 게임 루프는 10 TPS 기준으로 상태를 갱신한다. | `TICK_INTERVAL=0.1`이 10 TPS 기준과 일치하고, 자동 종료 시 `game.ended` 이벤트가 생성된다. | `backend/game/game_loop.py`, `backend/test_game_loop.py` | N/A |
| R-23 | Controller Pi 연결 해제와 재연결은 최신 WebSocket 기준으로 관리된다. | 같은 `pi_id`가 재연결될 때 stale socket disconnect가 현재 연결을 제거하지 않는다. | `backend/game/connections.py`, `backend/test_connections.py` | N/A |

## 테스트 매핑

| 테스트 파일 | 검증 요구사항 |
| --- | --- |
| `frontend/src/screens/StartGuide.test.tsx` | R-01 |
| `frontend/src/api/backendGameClient.test.ts` | R-05, R-07, R-15 |
| `frontend/src/game/gameRules.test.ts` | R-02, R-11, R-12, R-13, R-14 |
| `frontend/src/game/playerRenderMetrics.test.ts` | R-11 |
| `frontend/src/game/mapRenderers/sharedMapDrawing.test.ts` | R-11, R-12, R-13, R-14 |
| `frontend/src/game/mapRenderers/practiceRenderer.test.ts` | R-11 |
| `frontend/src/game/playerStatusEffects.test.ts` | R-11 |
| `frontend/src/assets/game/playerSprites.test.ts` | R-11 |
| `frontend/src/match/matchProgress.test.ts` | R-16 |
| `frontend/src/screens/SetupScreen.test.tsx` | R-02, R-04, R-10 |
| `frontend/src/screens/GamePlayScreen.test.tsx` | R-11 |
| `frontend/src/screens/ResultScreen.test.tsx` | R-04, R-15, R-16 |
| `backend/test_main_routes.py` | R-06 |
| `backend/test_pose_action.py` | R-09 |
| `backend/test_game_loop.py` | R-03, R-10, R-15, R-20, R-22 |
| `backend/test_connections.py` | R-08, R-23 |
| `backend/test_physics.py` | R-11, R-12, R-13, R-14 |
| `backend/test_runbook_contract.py` | R-19, R-21 |

## 테스트 기반 세부 검증 항목

아래 표는 요구사항 ID를 변경하지 않고, 자동 테스트가 실제로 검증하는 세부 조건을 요구사항별로 풀어 쓴 것입니다. 평가 Agent는 이 표를 코드·테스트·결과 매핑의 보조 근거로 사용합니다.

### 기능 요구사항 세부 검증

| 요구사항 ID | 세부 검증 항목 | 대응 테스트 |
| --- | --- | --- |
| R-01 | 시작 화면에 `2D LG Guys`, `조작 가이드`, 좌우 이동/점프/하단 점프/두 손 모으기 잡기 안내가 표시된다. Backend Ready 상태에서 시작 버튼을 누르면 시작 액션이 1회 호출되고, Server Disconnected 상태에서는 시작 버튼이 비활성화된다. | `frontend/src/screens/StartGuide.test.tsx` |
| R-02 | 일반 맵 카드 목록은 꼬리잡기, 땅따먹기, 별모으기를 제공하고, 연습장은 별도 버튼으로 노출된다. 맵 타입별 렌더 레이아웃은 backend map type과 동일한 키(`tail_tag`, `territory_capture`, `star_collect`, `practice`)로 조회된다. | `frontend/src/screens/SetupScreen.test.tsx`, `frontend/src/game/gameRules.test.ts` |
| R-03 | `setup_game(mapId, playerCount)` 호출 시 선택 인원 수만큼 `players`와 `controllers`가 생성되고, 초기 컨트롤러 상태는 `disconnected`, `lastSeenAt=null`, `player.connected=false`가 된다. | `backend/test_game_loop.py` |
| R-04 | 활성 플레이어 닉네임 입력 변경 시 해당 `playerId`와 새 닉네임이 변경 핸들러로 전달되고, 결과 화면과 컨트롤러 HUD에는 기본명 대신 편집된 닉네임이 표시된다. | `frontend/src/screens/SetupScreen.test.tsx`, `frontend/src/screens/ResultScreen.test.tsx`, `frontend/src/screens/GamePlayScreen.test.tsx` |
| R-05 | 프론트엔드 API 클라이언트가 `/health`, `/maps`, `/game`, `/game/setup`, `/game/nicknames`, `/game/start`, `/game/reset`을 올바른 HTTP method와 JSON body로 호출한다. | `frontend/src/api/backendGameClient.test.ts` |
| R-06 | 백엔드 `main.py`에 `GET /health`, `GET /maps`, `GET /game`, `POST /game/setup`, `POST /game/nicknames`, `POST /game/start`, `POST /game/reset` 라우트가 선언되어 있다. | `backend/test_main_routes.py` |
| R-07 | 프론트엔드 클라이언트가 `ws://<backend>/ws/game`에 연결하고, 수신한 JSON `GameState`를 파싱해 listener에 전달한다. 종료 이벤트가 포함된 상태는 `GameEndedEvent`로 변환된다. | `frontend/src/api/backendGameClient.test.ts` |
| R-08 | `pi_1..pi_4` 컨트롤러 연결 상태를 관리하고, pose stream 명령을 특정 컨트롤러에 전송할 수 있다. 전송 실패한 컨트롤러 소켓은 제거된다. | `backend/test_connections.py` |
| R-09 | 어깨 기울기는 좌우 이동과 `lean_right` 등 pose로 변환된다. 손목 또는 팔꿈치가 어깨보다 높으면 jump, 낮은 어깨는 duck, hand landmark 기반 두 손 모으기는 grab으로 변환된다. hand landmark가 부족할 때는 기존 keypoint 기반 fallback을 유지하고, 낮은 confidence keypoint는 입력으로 사용하지 않는다. | `backend/test_pose_action.py` |
| R-10 | 선택된 컨트롤러가 하나라도 미연결이면 게임 시작이 거절되고 상태는 `waiting`으로 유지된다. 선택된 모든 컨트롤러가 연결되면 게임 상태가 `countdown`으로 전환된다. | `backend/test_game_loop.py`, `frontend/src/screens/SetupScreen.test.tsx` |
| R-11 | GameState의 닉네임, 점수, 컨트롤러 연결 상태, 카메라 feed URL, 무제한 연습장 시간, freeze/cooldown 상태, gameplay event 효과음, Phaser 좌표 변환과 맵 렌더 보조값이 화면 렌더링에 반영된다. | `frontend/src/screens/GamePlayScreen.test.tsx`, `frontend/src/game/*.test.ts`, `frontend/src/game/mapRenderers/*.test.ts` |
| R-12 | 꼬리잡기에서는 꼬리 보유 여부에 따라 점수를 계산하고, 꼬리잡기 grab에는 비꼬리맵 freeze cooldown이 적용되지 않는다. | `backend/test_physics.py`, `frontend/src/game/mapRenderers/tailTagRenderer.ts` |
| R-13 | 땅따먹기에서는 5x5 타일 초기 렌더 fallback이 중립 소유자로 구성되고, 플레이어가 타일 위에 착지한 경우에만 점령 판정이 발생한다. 맵 전용 움직이는 발판과 wormhole 레이아웃이 적용된다. | `backend/test_physics.py`, `frontend/src/game/gameRules.test.ts` |
| R-14 | 별모으기에서는 별 수집 수를 점수로 반영하고, 별 위치/스폰 포인트와 별 렌더 반지름이 검증된다. 별모으기와 꼬리잡기 레이아웃은 서로 다른 fixed platform 구성을 가진다. | `backend/test_physics.py`, `frontend/src/game/mapRenderers/sharedMapDrawing.test.ts` |
| R-15 | 백엔드 종료 이벤트에는 `reason`과 `rankings`가 포함된다. 프론트엔드는 종료 이벤트의 rankings를 라운드 결과 목록과 podium/score card에 표시하고, 다음 맵 선택 액션을 제공한다. | `backend/test_game_loop.py`, `frontend/src/api/backendGameClient.test.ts`, `frontend/src/screens/ResultScreen.test.tsx` |
| R-16 | 라운드별 점수는 같은 `playerId`의 누적 점수에 더해지고, 최종 순위는 누적 점수 내림차순 및 동점 시 player slot 오름차순으로 정렬된다. 최종 결과 화면에는 home 액션만 표시된다. | `frontend/src/match/matchProgress.test.ts`, `frontend/src/screens/ResultScreen.test.tsx` |
| R-17 | reset 액션은 백엔드 `/game/reset`을 호출하고, 프론트엔드 match progress와 결과 snapshot을 초기화한 뒤 setup 또는 start 화면으로 복귀한다. | `frontend/src/App.tsx`, `frontend/src/api/backendGameClient.test.ts`, `backend/main.py` |
| R-23 | 같은 `pi_id`가 재연결되었을 때 이전 stale WebSocket disconnect가 최신 연결을 제거하지 않는다. 연결 sync는 살아 있는 컨트롤러를 connected로 복원하고 더 이상 살아 있지 않은 컨트롤러를 disconnected로 표시한다. | `backend/test_connections.py`, `backend/test_game_loop.py` |

### 비기능 및 검증 요구사항 세부 검증

| 요구사항 ID | 세부 검증 항목 | 대응 테스트/근거 |
| --- | --- | --- |
| R-18 | 프론트엔드 자동 테스트는 시작/설정/게임/결과 화면, API 클라이언트, match progress, Phaser 렌더 보조값, 카메라 feed, freeze/cooldown UI, sprite 렌더 안정성을 포함한다. `npm run test` 결과 12개 테스트 파일과 41개 테스트가 통과해야 한다. | `test-results/program_frontend/test-summary.md` |
| R-19 | TypeScript project build와 Vite production build가 오류 없이 완료되어야 한다. `.nvmrc`, `frontend/package.json` engines, `frontend/package-lock.json`이 Node.js 18.20.4 기준을 명시해야 한다. | `.nvmrc`, `frontend/package.json`, `frontend/package-lock.json`, `test-results/program_frontend/test-summary.md`, `backend/test_runbook_contract.py` |
| R-20 | 백엔드 Python 소스는 compileall을 통과해야 하고, route declaration, controller lifecycle, game loop, pose action, physics/rules, runbook contract 테스트가 모두 통과해야 한다. 현재 기준 unittest 전체가 통과해야 한다. | `test-results/program_backend/test-summary.md` |
| R-21 | `RUN.md`는 의존성 목록, 버전 고정 파일, 설치/빌드 명령, 실행 순서, 환경 변수, 외부 자원, 테스트 명령을 포함해야 한다. `.env.local.example`, lockfile, requirements, `.nvmrc` 파일이 함께 있어야 하며 자동 테스트가 이를 검증해야 한다. | `RUN.md`, `.nvmrc`, `frontend/.env.local.example`, `frontend/package-lock.json`, `backend/requirements.txt`, `benchmark/requirements.txt`, `backend/test_runbook_contract.py` |
| R-22 | 백엔드 게임 루프는 `TICK_INTERVAL=0.1`로 10 TPS 기준을 유지하고, 자연 종료 시 `game.ended` callback이 `time_limit` reason으로 호출되어야 한다. | `backend/game/game_loop.py`, `backend/test_game_loop.py` |
